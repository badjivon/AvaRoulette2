<%= simple_form_for(@ava) do |f| %>
  <%= f.input :photo, as: :file %>
  <%= f.input :number %>
  <%= f.input :batch %>
  <%= f.input :rare, collection: ["Commune", "Rare", "Légendaire", "Épique"] %>
  <%= f.button :submit %>
<% end %>
